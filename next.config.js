/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  // Enable static exports if needed for better compatibility
  // output: 'export',
  // trailingSlash: true,
}

module.exports = nextConfig;
